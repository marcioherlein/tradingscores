[project]
name = "tradingscores"
version = "0.1.0"
description = "Live decision-support stock screener for NYSE and NASDAQ"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "pandas>=2.2.0",
  "yfinance>=0.2.54",
  "numpy>=1.26.0"
]

[tool.vercel]
app = "api/index.py"
